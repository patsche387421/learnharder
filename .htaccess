# Cache-Control für JSON-Lerninhalte
# Stellt sicher, dass Änderungen an Lerninhalt-Dateien sofort sichtbar sind,
# ohne dass der Browser eine veraltete Version aus dem Cache liefert.
<FilesMatch "\.json$">
  Header set Cache-Control "no-cache, must-revalidate"
</FilesMatch>

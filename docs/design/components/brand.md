# Brand & Design Tokens

**Typ:** Komponente (Style-Guide / Referenz)
**Datei:** `brand.html`

## Kurzbeschreibung
Brand-Referenz: Farbpalette, Typografie-Skala, Abstände, Eckenradien als
visuelle Übersicht — entspricht dem Inhalt von `DESIGN_GUIDELINES.md`.

## Verwendete Tokens (aus `src/css/tokens.css`)
- **Alle** Tokens werden gezeigt: `--bg`, `--surface`, `--surface-2`,
  `--primary`, `--primary-hover`, `--text`, `--text-muted`, `--success`,
  `--error`, `--radius`.

## Verwendet auf `src/`-Seiten
- Keine produktive Seite — reine Referenz/Doku.

## TODOs / Abweichungen vom aktuellen Stand
- Mockup zeigt zusätzliche Akzentfarben, die in `tokens.css` (noch) nicht
  existieren — vor Übernahme: Token in `tokens.css` ergänzen +
  in `DESIGN_GUIDELINES.md` dokumentieren.
- Light-Mode-Varianten der Farbplättchen ergänzen.
- Diese Datei langfristig als `src/brand.html` (intern, nicht verlinkt)
  veröffentlichen? — offen.
